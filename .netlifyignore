# Dossier source masters (~47MB). Runtime uses thumbs/ + portraits/ WebP only.
dossier/*.png
dossier/_thumb_sizes.txt

# Local scratch — never publish
.tmp/

# Gated assets — served via /api/protected-media/ after login (see generate script)
.netlifyignore-protected.generated
